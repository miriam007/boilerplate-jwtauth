import React, { Component } from 'react';

// start of component and export all in one line
export default class Home extends Component {
    render() {
        return (
            <div>
                Home Component
            </div>
        )
    }
}